server:
  port: 8885
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost/eureka/
spring:
  application:
    name: feign-consumer
  zipkin:
    base-url: http://localhost:9411
  boot:
    admin:
      client:
        url: http://localhost:8889
feign:
  hystrix:
    enabled: true